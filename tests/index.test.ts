import { game, isGameOver } from "../src/index";
import { boat } from "../src/homepage"
import { gameBoard } from "../src/gameBoard";


test('should return true', () => {
    let board = gameBoard.createBoard()
    let enemy = gameBoard.createBoard()
    let ship = [{length: 0, hits:0, isSunk:false}]
    let ship2 =[ {length: 0, hits:0, isSunk:false}]
})

