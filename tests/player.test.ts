import { players } from "../src/player";
import {gameBoard} from "../src/gameBoard"

// test('ai will generate a random move on the board and it will be marked occupied',()=>{
//     let array= gameBoard.createBoard()
//     expect(players.ai(array)).toBe()
// })